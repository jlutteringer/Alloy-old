package org.vault.extensibility.context.merge.handlers;

import org.w3c.dom.Node;

public class MergeHandlerAdapter extends AbstractMergeHandler {
	public void merge(Node source, Node patch) {
		// Do nothing
	}
}
